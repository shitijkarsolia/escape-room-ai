[project]
name = "escape-room-ai"
version = "1.0.0"
description = "AI-powered escape room game with dynamic puzzle generation"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "openai",
    "pillow",
    "gunicorn",
    "python-dotenv",
]
